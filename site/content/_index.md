---
title: "minicode — structural context for coding agents"
description: "minicode indexes your codebase into a symbol graph at startup, then sends the model exactly the dependency cone it needs — not a dump of every related file. Works with local models (LM Studio, Ollama) and hosted models (OpenRouter, Anthropic, OpenAI)."
meta_image: "/images/minicode-ui.webp"
---
