baseURL = '/'
languageCode = 'en-us'
title = 'claudepanion'
enableRobotsTXT = true
disableKinds = ['taxonomy', 'term']

[params]
  description = 'A localhost host for AI-scaffolded personal software. One process, one URL, one user — Claude Code as the runtime backend.'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
